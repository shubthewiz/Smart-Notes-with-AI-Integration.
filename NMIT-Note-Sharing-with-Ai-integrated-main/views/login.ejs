<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: #fde7d0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* Main Card */
    .container {
      display: flex;
      width: 900px;
      height: 520px;
      background: white;
      border-radius: 25px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
      overflow: hidden;
    }

    /* Left Side */
    .left {
  flex: 1;
  background: linear-gradient(135deg, #2f855a, #38a169);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.login-img {
  width: 85%;
  max-width: 380px;
  animation: float 3s ease-in-out infinite, glow 2s ease-in-out infinite alternate;
  filter: drop-shadow(0 10px 20px rgba(0,0,0,0.25));
  transition: transform 0.3s ease;
}

/* Floating animation */
@keyframes float {
  0%,100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-12px);
  }
}

/* Soft glow animation */
@keyframes glow {
  from {
    filter: drop-shadow(0 5px 10px rgba(255,255,255,0.4));
  }
  to {
    filter: drop-shadow(0 15px 30px rgba(255,255,255,0.7));
  }
}

/* Hover zoom effect */
.login-img:hover {
  transform: scale(1.05) rotate(1deg);
}


    /* Right Side */
    .right {
      flex: 1.3;
      padding: 60px 50px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .right h2 {
      font-size: 30px;
      margin-bottom: 10px;
      color: #1a202c;
    }

    .right p {
      font-size: 14px;
      color: #6b7280;
      margin-bottom: 30px;
      line-height: 1.4;
    }

    .input-box {
      width: 100%;
      margin-bottom: 18px;
    }

    .input-box input {
      width: 100%;
      padding: 14px 20px;
      border-radius: 30px;
      border: none;
      background: #f3f4f6;
      font-size: 14px;
      outline: none;
    }

    .btn {
      margin-top: 10px;
      padding: 14px;
      background: #2f855a;
      color: white;
      border-radius: 30px;
      border: none;
      font-size: 15px;
      font-weight: 500;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .btn:hover {
      background: #276749;
      transform: scale(1.02);
    }

    .social {
      display: flex;
      justify-content: center;
      gap: 15px;
      margin-top: 25px;
    }

    .social div {
      width: 40px;
      height: 40px;
      background: #f3f4f6;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .social div:hover {
      background: #e2e8f0;
    }

    .signup {
      text-align: center;
      margin-top: 20px;
      font-size: 13px;
      color: #4b5563;
    }

    .signup a {
      color: #2f855a;
      font-weight: 500;
      text-decoration: none;
    }

    @media (max-width: 768px) {
      .container {
        flex-direction: column;
        height: auto;
      }
      .left {
        height: 250px;
      }
    }
  </style>
</head>

<body>

<div class="container">

  <!-- Left Illustration -->
  <div class="left">
  <img src="/images/loginimg.jpg" alt="Cartoon" class="login-img">
</div>


  <!-- Right Login Section -->
  <div class="right">
    <h2>Log in</h2>
    <p>Hello, friend! I’m your smart assistant – let’s get started 🚀</p>

    <form method="POST" action="/login">
      <div class="input-box">
        <input type="email" name="email" placeholder="Email" required>
      </div>

      <div class="input-box">
        <input type="password" name="password" placeholder="Password" required>
      </div>

      <button class="btn" type="submit">Let’s start!</button>
    </form>

    <div class="social">
      <div><a href="/auth/facebook">f</a></div>
      <div><a href="/auth/google">G</a></div>
    </div>

    <div class="signup">
      Don’t have an account? <a href="register">Sign up</a>
    </div>
  </div>

</div>

</body>
</html>
